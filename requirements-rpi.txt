# ── Core Vision (headless, RPi 최적) ─────────────────────────────────────────
numpy>=1.26.0
opencv-python-headless>=4.9.0   # X11 불필요, RPi에서 opencv-python보다 가볍다

# ── Deep Learning ─────────────────────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
ultralytics>=8.2.0        # YOLOv8 Nano (CPU fallback용)

# ── Hardware I/O ──────────────────────────────────────────────────────────────
smbus2>=0.4.3             # VL53L1X I²C 버스 접근
VL53L1X>=0.0.5            # pimoroni ToF 센서 드라이버 (ctypes 패치 필요)

# ── Audio Output ──────────────────────────────────────────────────────────────
sounddevice>=0.4.6        # ALSA 기반 3.5mm 잭 오디오 출력

# ── TTS (Text-to-Speech) ──────────────────────────────────────────────────────
# espeak-ng: Python 패키지가 아닌 시스템 패키지이므로 pip 설치 불가
# sudo apt install espeak-ng espeak-ng-data-ko

# ── GPIO ──────────────────────────────────────────────────────────────────────
gpiod>=2.0.0              # libgpiod Python 바인딩 (버튼 핸들러)
# 시스템 패키지 선행 설치 필요: sudo apt install libgpiod-dev libgpiod2

# ── NPU (Orange Pi 5 전용) ────────────────────────────────────────────────────
# rknnlite2: Armbian 기본 탑재 (/usr/lib/python3/dist-packages/rknnlite)
# 또는 Rockchip 공식 wheel:  pip install rknnlite2
# (pip으로 설치 불가한 경우 시스템 경로 직접 참조)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
