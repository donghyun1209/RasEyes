//
//  Secrets.example.swift
//  RasEyesNav
//
//  이 파일을 `Secrets.swift`로 복사한 뒤 실제 키를 채운다.
//  `Secrets.swift`는 .gitignore에 등록되어 커밋되지 않는다.
//

enum Secrets {
    /// TMAP Open API 앱 키.
    /// https://openapi.sk.com 가입 → 앱 등록 → 앱 키 복사 (결제 수단 등록 불필요)
    static let tmapAppKey = ""
}
